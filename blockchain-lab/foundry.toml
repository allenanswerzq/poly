[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.20"

# Enable optimizer
optimizer = true
optimizer_runs = 200

# Verbose output for tests
verbosity = 2

# Gas reporting
gas_reports = ["*"]

[profile.default.fuzz]
runs = 256

[fmt]
line_length = 100
tab_width = 4
